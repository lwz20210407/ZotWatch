requests>=2.31
pydantic>=2.6
PyYAML>=6.0.1
rapidfuzz>=3.5
jinja2>=3.1
feedparser>=6.0
faiss-cpu>=1.7
numpy>=1.24
# Embeddings come from an OpenAI-compatible HTTP API (config/embedding.yaml), so
# sentence-transformers and its ~2 GB torch dependency are NOT needed in CI.
# Install it only to run with provider: local
#   pip install "sentence-transformers>=2.2"
python-dotenv>=1.0
# zoneinfo has no tz database on Windows or in slim containers.
tzdata>=2024.1; platform_system == "Windows"
